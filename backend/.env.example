PORT=3000
NODE_ENV=development
# Si usas Supabase, define aquí la URL de tu proyecto y la Service Role key (no la subas al repo)
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Alternativamente puedes usar DATABASE_URL si prefieres conectar directamente al Postgres
# DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

UPLOADS_DIR=./uploads
FRONTEND_URL=http://localhost:4300

# Copia este archivo a .env y rellena las variables antes de ejecutar los scripts.# Copia este archivo a .env y completa DATABASE_URL con la conexión a Postgres (Supabase/Neon/Railway)
DATABASE_URL="postgresql://user:password@host:5432/dbname"
